# Ensure consistent installs across OSes
engine-strict=true
auto-install-peers=true
legacy-peer-deps=false
fund=false

# Always save exact versions (no ^ or ~)
save-exact=true

# Avoid permission issues on global installs
prefix=${HOME}/.npm-global

